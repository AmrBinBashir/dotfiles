super + Return
	kitty
alt + Tab
	rofi -show window
alt + space
	rofi -show drun
super + r
	rofi -show run
super + space
	(setxkbmap -query | grep -q "layout:\s\+us") && setxkbmap ar || setxkbmap us
alt + l
	dm-tool lock
super + shift + s
	flameshot gui