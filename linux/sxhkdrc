super + Return
	kitty
alt + Tab
	rofi -show window
alt + space
	rofi -show drun
super + r
	rofi -show run
super + space
	dbus-send --dest=ru.gentoo.KbddService /ru/gentoo/KbddService ru.gentoo.kbdd.set_layout uint32:0i
alt + l
	dm-tool lock
